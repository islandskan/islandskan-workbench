{% extends "layouts/base.njk" %}

{% block content %}

<section class="thought">
    <h2>{{title}}</h2>
    <p>{{date | toStringDate}}</p>
    <article>
        {{content | safe}}
    </article>
</section>

{% endblock %}
