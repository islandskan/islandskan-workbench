<div class="card {{_cardType}}">
    {% if _item.data %}
    {# item is from collection #}
    <h5><a href="{{_item.url}}">{{_item.data.title}}</a></h5>
    {% else %}
    {# item is from bookshelf.json #}
        <h6>{{_item.title}}</h6>
        {% if _item.relatedThought %}
            <a href="{{_item.relatedThought}}">Read my thoughts on this</a>
        {% endif %}
{% endif %}
</div>
